We investigate applying decision forests to classification problems in the latent space of a VAE. We use the distribution over the latent space to approximate gradients and tune the model using SGD. 
